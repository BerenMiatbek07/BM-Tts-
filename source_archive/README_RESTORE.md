# Source archive transfer

The two `.b64` files currently in this folder are incomplete and are not the final source package.

The actual project source is being uploaded as readable files to the repository root. Use the repository root files for the Windows build.
